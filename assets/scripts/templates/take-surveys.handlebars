{{#each surveys as |survey|}}
  {{log survey}}

  <section class="survey-info" data-id="{{survey._id}}">
<!-- Update Quote Modal -->
<form class="take-survey-form">
<h3>{{survey.title}}</h3>
<div class="form-check">
  <p><input class="form-check-input" type="radio" value="{{survey.option1}}" name="answer" id="response1-{{survey._id}}" value="{{survey.option1}}">
  <label class="form-check-label" for="defaultCheck1">
    {{survey.option1}}
  </label></p>
</div>
<div class="form-check">
<p><input class="form-check-input" type="radio" value="{{survey.option2}}" name="answer" id="response2-{{survey._id}}" value="{{survey.option2}}">
  <label class="form-check-label" for="defaultCheck1">
    {{survey.option2}}
  </label></p>
</div>
<div class="form-check">
  <p><input class="form-check-input" type="radio" value="{{survey.option3}}" name="answer" id="response3-{{survey._id}}" value="{{survey.option3}}">
  <label class="form-check-label" for="defaultCheck1">
    {{survey.option3}}
  </label></p>
</div>
<div class="form-check">
  <p><input class="form-check-input" type="radio" value="{{survey.option4}}" name="answer" id="response4-{{survey._id}}" value="{{survey.option4}}">
  <label class="form-check-label" for="defaultCheck1">
    {{survey.option4}}
  </label></p>
</div>
<div class="form-check">
  <p><input class="form-check-input" type="radio" value="{{survey.option5}}" name="answer" id="response5-{{survey._id}}" value="{{survey.option5}}">
  <label class="form-check-label" for="defaultCheck1">
    {{survey.option5}}
  </label></p>
</div>
  <p><button type="submit" class="btn btn-info">Submit Response</button>
  </button></p>
</form>

  </section>

{{/each}}
